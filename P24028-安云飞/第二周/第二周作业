1.Python垃圾回收机制
    在Python中，对数据引用计数器，当引用计数器为0时启用垃圾回收。
    Python 是通过引用计数和一个能够检测和打破循环引用的循环垃圾回收器来执行垃圾回收的

2.斐波那契数列、素数、质数、猴子吃桃问题
    斐波那契数列：数列从第3项开始，每一项都等于前两项之和
    素数：指在大于1的自然数中，除了1和它本身以外不再有其他因数的自然数
    质数：指在大于1的自然数中，除了1和它本身以外不再有其他因数的自然数
    猴子吃桃问题：猴子吃桃问题：猴子第一天摘下若干个桃子，当即吃了一半，还不过瘾，又多吃了一个，
                第二天早上又将剩下的桃子吃掉一半，又多吃了一个。以后每天早上都吃前一天剩下的一半零一个。
                到第10天早上想再吃时，见只剩下一个桃子了。求第一天共摘多少个桃子？

3.列表支持的所有方法和说明
    list.append() --> 尾部添加
    list.clear() --> 清空列表
    list.copy() --> 浅复制列表
    list.count(value) --> 返回列表内匹配value元素的数量
    list.extend() --> 将可迭代的元素追加进来
    list.pop(index) --> 删除指定位置的元素，若没有指定则删除尾部元素
    list.remove(value) --> 从左至右删除第一个匹配value的元素
    list.insert(index, value) --> 指定位置添加value
    list.reverse() --> 将列表元素反转
    list.sort() --> 对列表元素进行排序，默认升序

4.简易计算器
    见test.py文件