1. 理解并发 与 并行
    并行：
        同一时刻，同时进行任务工作，任务之间相互不干扰。任务同时进行
    并发：
        一段时间内，完成某些任务。任务交替进行

2. 理解线程 与 进程，并说明它们的应用场景
    线程：
        线程是任务的执行单位，是操作系统能进行运行调度的最小单位
    进程：
        程序被操作系统加载到内存中，就是进程。进程中存放着指令和数据。一个进程可以包含多个线程

    应用场景：
        cpu密集型时，由于GIL锁原因，不能充分发挥多线程效率。所以应采用多进程
        IO密集型时，多线程效率较高，应采用多线程

3. Python中的GIL是什么，它影响什么
    GIL全局解释器锁：
        GIL保证Cpython进程中，只有一个线程执行字节码。甚至多核CPU的情况下，也只允许一个CPU上的一个线程运行

4. 结合本周知识现实如下功能：
   创建两个线程，其中一个输出1-26，另外一个输出A-Z
   要求最终程序输出格式为： 1A 2B 3C ...

   见附件