1. 使用本周和之前所学习的知识实现cat命令（支持查看内容和-n参数功能即可）
    答：参见cat.py文件

2. 有字符串”not 404 found 张三 99 深圳”，使用正则过滤掉英文和数字，最终得到”张三 深圳”
    答：[^a-z\d\s]{2,3}

3. 结合本周所讲的知识，总结如何设计一个日志分析系统，分析可能遇到问题和解决方法（文字阐述）。
    答：
    思路：
        1.使用正则表达式，匹配出需要的内容
        2.对匹配内容进行处理包装（处理包装成后面要用的数据格式）
        3.使用queue队列，将数据put出去，后面用get获取数据
        4.使用窗口方式，对数据进行缓存后处理
        5.可以使用多线程，对没个线程进行数据队列发送，获取，缓存，处理

        困难点：还是要能够把需求细化处理，整体程序架构思路不明确，数据处理的方式不清楚。知识点没有能够融汇贯通
