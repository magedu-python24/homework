1. 简要说明Python垃圾回收机制
   在Python中，主要通过引用计数进行垃圾回收；
   通过 “标记-清除” 解决容器对象可能产生的循环引用问题；
   通过 “分代回收” 以空间换时间的方法提高垃圾回收效率。

2. 什么是斐波那契数列、素数、质数和猴子吃桃问题（文字说明即可）？
   a.斐波那契数列：从第三项开始，每一项都等于它前一项与它的前一项的前一项的和，用函数表示为f(n)=f(n-1)+f(n-2) (n>2,n∈N*）
   b.素数: 也称质数,一个数如果只被自己和1整除，那它就是质数，即素数。
   c.猴子吃桃问题:猴子第一天摘下若干个桃子，当即吃了一半，还不过瘾，又多吃了一个。第二天早上又将剩下的桃子吃掉一半，又多吃了一个。
                 以后每天早上都吃了前一天剩下的一半零一个。到第十天早上再想吃时，就只剩一个桃子了。求第一天共摘了多少个桃子？

　
3. 请写出列表支持的所有方法及说明（例如: append 向列表末尾添加元素）
    说明：函数中的参数index表示下标，'xxx'表示元素，listname列表的名称，len步长
    1.初始化列表 例：listname = []
    2.追加
           用法：listname.append('xxx')  在末尾添加
    3.删除
           用法：
                1）listname.pop()   如果没有指定下标，则默认删除最后一个
                2）listname.remove(index)    删除对应下标的元素
                3）del listname[index]    删除对应下标的元素
    4.查找元素所在位置
           用法：listname.index('xxx')
    5.统计元素的次数
           用法：listname.count('xxx')
　  6.反转
           用法：listname.reverse()
    7.插入
           用法：
                1）listname.insert(index, 'xxx')
                2）listname[index] = 'xxx'
    8.排序
           用法：listname.sort()  该排序是按照ASCII码来进行排序的
    9.拼接两个列表
           用法：listname.extend(listname2)
    10.对列表进行切片
           用法：
                1）全取 listname[::]
                2）部分取 listname[index1:index2]
                3) 按步长去 listname[index1:index2:len]
           说明：实际操作中取不到index2下标所对应的值，即所谓的顾头不不顾尾
    11.复制
           用法：listname.copy() 也可以使用上面的切片中的全取操作

4. 实现一个简易的计算器，效果如下：　
    （1）. 运行后提示让用户输入一个数字　
    （2）. 提示输入操作符（+ - * /）　
    （3）. 再次提示输入一个数字　
    （4）. 打印计算结果　
    （5）. 在不退出程序的前提下，可以允许用户继续输入新一组数据计

    源程序详见homework.py