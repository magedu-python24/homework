1. linux distrubution
redhat, centos, ubuntu, debian, suse, arch.
2. executeing commands as below
    a. ssh zhangsan@192.168.139.100 -p 22
    b. mkdir /data
    c. touch /data/a.sh
    d. echo "hello magedu" > /data/a.sh
    f. rm -rf /data
    g. exit